# Connection string for the PostgreSQL database
# Replace <USER>, <PASSWORD>, <HOST>, <PORT>, and <DATABASE_NAME> with your actual database credentials
DATABASE_URL=postgresql://<USER>:<PASSWORD>@<HOST>:<PORT>/<DATABASE_NAME>

# Secret key used to sign and verify JSON Web Tokens (JWT)
# Replace <SECRET> with a strong, random string to ensure security
JWT_SECRET=<SECRET>

# Number of salt rounds used by bcrypt for hashing passwords
# Replace <NUMBER> with an integer value, typically between 10 and 14
BCRYPT_SALT_ROUNDS=<NUMBER>
